# AGENTS.md

## Cursor Cloud specific instructions

This repository (`sate-image-query`) is a newly initialized project with only a `README.md`. No application code, dependencies, build system, or services exist yet.

### Current state
- **Language / Framework**: Not yet determined.
- **Dependencies**: None.
- **Services**: None to start.
- **Lint / Test / Build**: No tooling configured yet.

### Notes for future agents
- When code and dependencies are added, update this file and the VM update script accordingly.
- The update script is currently a no-op (`echo "No dependencies to install"`). Replace it once a package manager and lockfile are introduced.
